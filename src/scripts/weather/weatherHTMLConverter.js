export const weatherHTMLConverter = (weatherObj) => {
    // const target = document.querySelector('.top-row__current-weather')
    // target.innerHTML = `
    // <h1>CURRENT WEATHER</h1>
    // Today is: ${weatherObj[0].main}
    // `
}